apiVersion: v1
kind: Service
metadata:
  name: {{ include "spark_app.fullname" . }}
  labels:
    {{- include "spark_app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "spark_app.selectorLabels" . | nindent 4 }}
